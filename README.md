# agent-scratch

Monorepo layout for mixed project types.

## Layout

- `apps/`: user-facing applications
- `services/`: backend services and workers
- `libs/`: shared libraries and packages
- `tools/`: developer tools, scripts, and internal utilities

## Current Projects

- `tools/codex-log/`: inspect Codex local SQLite websocket request and event payloads

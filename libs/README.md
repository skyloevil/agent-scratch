# libs

This directory is intended for shared code that can be reused across apps, services, and tools.

Typical examples:

- domain models
- API clients
- shared utility packages
- configuration helpers
- test support libraries

## Current Status

No shared libraries have been added yet.

When adding a library here, document:

- its public API surface
- who depends on it
- versioning or compatibility expectations
- how it is tested
